#include<bits/stdc++.h>
using namespace std;

int maxLen(vector<int>&A, int n)
    {   
        
    }

int main(){ 
    vector<int> v=  {15,-2,2,-8,1,7,10,23};
    int result = maxLen(v , v.size());
    cout<<result;

     return 0;
}